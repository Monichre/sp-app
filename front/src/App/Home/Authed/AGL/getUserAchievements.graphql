query GetUserAchievements($pk: String!, $fk: String!) {
	getUserAchievements(pk: $pk, fk: $fk) {
		total
		artist {
			name
			genres
			images {
				url
			}
			external_urls {
				spotify
			}
		}
		user {
			uid
			email
			displayName
			lastUpdate
			photoURL
		}
	}
}